# Kana Chain Block Code 最終仕様（Algorithm 1 / Dictionary 1）

仮名チェーンブロック符号（Kana Chain Block Code、KCBC）は、任意のバイト列を
日本語のしりとりへ可逆変換する。

この文書は、アルゴリズム版 `1` と辞書版 `1` の組（`A1/D1`）を、実装間で
相互運用できる水準で定義する。本文中の「かな」はひらがな1文字、「読み」は
符号表に載る正規化済みの単語を指す。

## 1. 設計上の不変条件

`A1/D1`は次を満たす。

- すべての可視語が、ブロック境界をまたぐ箇所も含めてしりとりでつながる
- 最初のかなはデータから選び、特定のかなへ固定しない
- 同じ読みは1ブロック内で高々1回だけ使う
- ブロックをまたぐ再使用は許す
- `もも`、`まま`、`りんり`のような回文や、先頭かなと末尾かなが同じ通常語を許す
- 2文字語だけへ偏らず、2文字・3文字・4文字以上の語が混ざるようにする
- 上の制約を守ったうえで、出力語数を最優先で小さくする
- 各ブロックがCRCを持ち、破損したブロック番号を特定できるようにする

回文や自己ループ語も、ほかの語と同じくブロック内では1回しか使えない。
したがって、同じ自己ループ語だけを同一ブロック内で繰り返すループは作れない。
直前ブロックで使った語を次のブロックで再び使うことは許される。

## 2. バージョン

### 2.1 アルゴリズム版

`algorithmVersion`は1 byteの符号なし整数である。この文書で定義するフレーム、
ブロック、rank写像、候補探索、正規出力の選択規則を版`1`とする。

次のいずれかを変え、既存の符号列の解釈が変わる場合は、新しい
`algorithmVersion`を割り当てなければならない。

- ストリームヘッダーまたはブロックフレーム
- CRCの種類、検査範囲またはbyte順
- 経路の数え方、rank順、rank写像または候補生成
- ブロック語数、ブロック内再使用規則または候補比較順

### 2.2 辞書版

`dictionaryVersion`も1 byteの符号なし整数である。第3章の現在の符号表を版`1`
とする。`states`、`normalWords`、`terminalWords`の内容または順序を変えた場合は、
新しい`dictionaryVersion`を割り当てなければならない。同じ版番号へ別の符号表を
割り当て直してはならない。

JSONの格納形式を表す`schemaVersion`は、符号上の辞書版とは別物である。

### 2.3 復号時の版選択

版番号は、最初のブロックから復元されるストリームヘッダー内にある。そのため、
版番号だけを読んでから経路復号を始めることはできない。単一版の実装は`A1/D1`を
既定として復号し、復元後に版番号を照合する。複数版を扱う実装は、呼び出し側から
組を指定させるか、対応する組を試して版番号とブロックCRCの両方が一致したものを
採用する。

埋め込まれた版番号は、誤ったアルゴリズムまたは辞書で復号した結果を受理しない
ための識別子である。

## 3. 符号表

実装は次の順序付きデータを持つ。

| 項目 | 内容 |
| --- | --- |
| `states` | 接続状態となるかなを並べた文字列 |
| `normalWords` | 通常語の読みを並べた配列 |
| `terminalWords` | `ん`で終わる終端語の読みを並べた配列 |
| `katakanaReadings` | カタカナで表示する読みの集合。符号には影響しない |
| `codebookSha256` | 符号表の内容と順序の同一性 |

制約は次のとおりである。

- すべての読みはNFC正規化済みのひらがなまたは長音記号からなる2〜8文字である
- `normalWords`の先頭かなと末尾かなはともに`states`に含まれ、末尾は`ん`ではない
- `terminalWords`の先頭かなは`states`に含まれ、末尾は`ん`である
- 2配列はそれぞれ重複せず、互いにも交わらない
- すべての状態から、通常語を0語以上経由して終端語へ到達できる

読み`w`は、先頭かなの状態から末尾かなの状態への辺とみなす。先頭かなと
末尾かなが同じ通常語も除外しない。単語が回文かどうかはrank規則に影響しない。

`codebookSha256`は`sha256:`に続く16進64桁で、次の空白なしJSONをUTF-8にした
バイト列のSHA-256である。配列順をそのまま使う。

```json
{"normal":["あい","あいさつ"],"terminal":["あいけん"]}
```

辞書版1の値は次のとおりである。

| 項目 | 値 |
| --- | --- |
| `dictionaryVersion` | `1` |
| `codebookSha256` | `sha256:823d3f3f48bf09e85447d61903a7b29cf6a774108745a1a4b5c228ba976327e5` |
| `states` | `あいうえおかがきぎくぐけげこごさざしじすずせぜそぞただちつてでとどなにぬねのはばぱひびぴふぶぷへべぺほぼぽまみむめもやゆよらりるれろわ` |
| 通常語 | 2,748語 |
| 終端語 | 428語 |
| 状態 | 67個 |

辞書版1には自己ループ通常語が46語あり、`もも`と`まま`も含む。アルゴリズムは
`りんり`のような読みも許容するが、実際に使える読みは対象辞書に載るものに限る。

## 4. ストリーム

入力から、次のストリームを作る。

```text
+------------------+-------------------+------+---------+
| algorithmVersion | dictionaryVersion | mode | payload |
| 1 byte           | 1 byte            | 1 byte | variable |
+------------------+-------------------+------+---------+
```

固定オーバーヘッドは3 bytesである。

| mode | payload | 復号後 |
| --- | --- | --- |
| 0 | 入力バイト列そのまま | payload |
| 1 | 入力バイト列のraw DEFLATE | inflate(payload) |
| 2 | 入力文字列のUTF-16LE | UTF-16LEからUTF-8へ変換 |
| 3 | 2のraw DEFLATE | inflate後にUTF-16LEからUTF-8へ変換 |

モード2と3は入力が文字列として与えられた場合だけ生成する。圧縮はRFC 1951の
raw DEFLATEとする。版1ではmodeの上位6 bitは0でなければならない。

ストリーム全体のCRCは持たない。第5章の各ブロックCRCが、ストリームの全byteを
重複なく検査する。

## 5. CRC付きブロック

### 5.1 データ分割

ストリームを先頭から最大18 bytesずつのデータ片へ分ける。最終ブロック以外は
原則として18 bytesを運ぶ。対象のデータ片を`data`、その長さを`length`、
0始まりのブロック番号を`index`とする。

ブロック種別`type`は、通常ブロックが`0`、最終ブロックが`1`である。

### 5.2 CRC-16/CCITT-FALSE

各ブロックの検査入力は次のバイト列である。

```text
type (1 byte)
|| index (4 bytes, big-endian)
|| length (1 byte)
|| data (length bytes)
```

CRCはCRC-16/CCITT-FALSEを使う。生成多項式は`0x1021`、初期値は`0xFFFF`、
入力・出力の反転なし、最終XORは`0x0000`である。`123456789`のASCII
バイト列に対する値は`0x29B1`である。

ブロックフレームは次のバイト列である。

```text
length (1 byte) || data || CRC-16 (2 bytes, big-endian)
```

フレーム長は`length + 3` bytes、bit数は`m = 8 * (length + 3)`である。
`type`と`index`は語列の位置から分かるためフレーム内には格納しないが、CRCの
検査範囲には含める。これにより、ブロックの置換・欠落・順序違いも検出する。

通常ブロックの`length`は1〜18、最終ブロックは0〜18とする。通常は18を使い、
第7章の適合経路が得られない場合だけ17から1まで順に短くする。

## 6. 生の経路とrank

### 6.1 経路数

`N(w,s)`を、状態`s`から始まり通常語だけをちょうど`w`語つないだ生経路数、
`T(w,s)`を、状態`s`から始まりちょうど`w`語目で終端語へ到達する生経路数とする。

```text
N(0, s) = 1
N(w, s) = sum N(w - 1, tail(e))
              e in normal(s)

T(0, s) = 0
T(1, s) = |terminal(s)|
T(w, s) = sum T(w - 1, tail(e))              (w > 1)
              e in normal(s)
```

ここで数える「生経路」では、単語の再使用をまだ除外しない。自己ループ語の連続も
数に含める。ブロック内重複禁止は第7章で候補を選ぶときの適合条件にする。これにより、
経路数の状態へ巨大な使用済み語集合を持ち込まずに済む。

### 6.2 rank順

状態`s`から出る通常語を`normalWords`の順に列挙する。終端経路の最終位置だけは、
その後へ`terminalWords`の順で終端語を列挙する。

rankと経路の対応は標準的な組合せ的順位付けで定める。各候補語について、その語を
選んだ場合の残り経路数を6.1から求め、rankから順に引いていく。復号方向では実際の
語より前にある候補の残り経路数を合計する。

ブロック先頭の状態は、最初の可視語の先頭かなから分かる。開始状態ごとに独立した
rank空間を使い、全状態を1つのrank空間へ連結しない。

## 7. 同じデータを表す候補経路

### 7.1 rank写像

あるブロックの生経路数を`C`、`b = floor(log2(C))`、
`S = 2^b`とする。第5章のブロックフレームをビッグエンディアン整数`f`とし、
`M = 2^m`とする。

上位`r = b - m` bitを候補番号`t`として、符号rank`x`を作る。

```text
x = t * M + f
rho = floor(x * C / S)
```

`rho`が6.2の生経路rankである。復号時は次で`x`を戻す。

```text
x = ceil(rho * S / C)
```

`floor(x * C / S) = rho`を満たさない経路、または`x >= S`となる経路は、
符号空間外として拒否する。ブロックフレームは`f = x mod M`で復元する。

同じ`f`に多数の`t`を割り当てることで、データを増やさずに別のしりとり経路を
選べる。

### 7.2 候補番号の生成

候補番号を小さい順に試すと、辞書順の近い経路へ偏る。そこで、次のバイト列の
SHA-256を候補番号の生成に使う。

```text
ASCII("KCBC-A1-PATH") || 0x00
|| type
|| index (4 bytes, big-endian)
|| UTF8(startState)
|| blockFrame
|| counter (4 bytes, big-endian)
```

SHA-256値をビッグエンディアン整数とし、下位`r` bitを`t`とする。同じ`t`が
再び出た場合は捨て、counterを増やす。

counterを0から増やし、重複を除いて**128個の異なる`t`**を作る。その128候補だけを
7.4で比較する。`t`はJavaScriptでも`Number`へ変換せず、多倍長整数のまま扱う。

通常ブロックで適合候補がなければ、8.2に従ってデータ片を1 byte短くして、新しい
ブロックフレームから128候補を作り直す。最終ブロックで適合候補がなければ、8.3に
従って語数を1語増やす。これにより探索回数を有限かつ実装間で同一にする。

### 7.3 適合経路

候補経路は次をすべて満たす場合だけ適合する。

- ブロック内の読みがすべて異なる
- 通常ブロックは通常語35語ちょうどである
- 最終ブロックは最後の1語だけが終端語である
- 先頭かなが指定された開始状態と一致する
- 前ブロックがある場合、開始状態が前ブロック末尾かなと一致する

自己ループ語は選べるが、同じブロックの使用済み集合へ入るため2回目は不適合になる。
新しいブロックでは使用済み集合を空にする。

### 7.4 語の多様性

読みを文字数で`L2`（2文字）、`L3`（3文字）、`L4+`（4〜8文字）へ分ける。
適合候補は次の順で比較する。

1. 3群の語数の最大値と最小値の差が小さい
2. それ以前のブロックですでに使った読みの個数が少ない
3. 2文字語が少ない
4. 読みの総文字数が多い
5. 生経路rankが小さい

1により、辞書内の個数ではなく長さの種類を基準に語を混ぜる。2は全体での再使用を
禁止せず、同条件ならまだ使っていない語を優先する。

## 8. ブロック構成

### 8.1 第1ブロックの開始かな

第1ブロックの開始状態は、同ブロックのCRC-16を使って次で決める。

```text
startState = states[CRC16(checkInput) mod |states|]
```

辞書版1では67状態のすべてが開始状態になり得る。固定の版番号やmodeではなく、
データ片、長さ、ブロック種別を含むCRCから選ぶため、常に`あ`から始まる偏りを
作らない。

第2ブロック以降の開始状態は、直前ブロックの末尾語の末尾かなである。開始状態を
自由に選び直してはならない。

### 8.2 通常ブロック

通常ブロックは35語の通常語からなる。符号器は残りストリームの先頭から18 bytesを
候補データ片とし、第5〜7章の適合経路を探す。存在しなければ17 bytes、16 bytesの
ように1 byteずつ短くし、最初に適合経路が得られた最大長を使う。

辞書版1の35語通常経路容量は、開始状態により201〜207 bitである。最大長18 bytesの
ブロックフレームは168 bitなので、最弱状態でも33 bit以上を候補選択へ使える。

### 8.3 最終ブロック

残りストリームが18 bytes以下になったら最終ブロックを作る。データ長は残り全部を
使う。

最終ブロックは、次を満たす最小の語数`w`を使う。

- 終端経路容量`b = floor(log2(T(w,s)))`が`m + 20` bit以上
- 7.2の候補探索で適合経路が得られる

20 bitの余剰により候補番号空間に十分な余裕を持たせ、その中からSHA-256で分散した
128候補を調べる。
`w`は35以下でなければならない。辞書版1では最大長18 bytesのフレームでも、
すべての開始状態で34語以下に収まる。最後の語だけが`ん`で終わる。

最終ブロックしかない場合の開始状態も8.1で決める。

### 8.4 ブロック数と語数の最小化

データ分割では、適合経路が存在する最大のデータ長を各通常ブロックへ入れる。
最終ブロックは8.3の最小語数を使う。さらに第10章の複数ストリーム候補を実際に
すべて符号化し、総語数が最小のものを選ぶ。

この規則は、ブロック内重複禁止と多様性のための余剰bitを確保した範囲で語数を
最小化する。

## 9. 復号

復号器はブロックを先頭から処理する。

1. 表示上の読みをひらがなへ正規化する
2. しりとり接続、語種、語数、ブロック内重複を検証する
3. 6.2に従って生経路rank`rho`を得る
4. 7.1に従って符号rank`x`を得る
5. 通常ブロックは`length = 1..18`、最終ブロックは`0..18`を試す
6. 各長さの`m`について`f = x mod 2^m`を復元し、先頭byteのlengthと照合する
7. type、index、length、dataからCRC-16を再計算する
8. lengthとCRCの両方が一致する候補がちょうど1つならdataを採用する

一致する候補がない場合、または複数ある場合は、そのブロックを不正とする。
エラーのブロック番号は`index`と同じ0始まりとし、必ずメッセージへ含める。
推奨メッセージは`CRC-16 mismatch in block N`である。

すべてのdataを連結したあと、先頭3 bytesの版番号とmodeを検証し、modeに従って
payloadを復元する。

ブロックの単語が壊れてrank不能になった場合、CRCまで到達しなくても同じブロックの
構造エラーとして報告する。あるブロックの末尾かなが壊れて次ブロックとの接続も
合わなくなった場合は、最初に不整合が見つかったブロックを報告する。

CRC-16は誤り検出であり、改ざん耐性を提供する認証ではない。

## 10. ストリーム候補の選択

符号器は入力から次の候補を作り、各候補を最後までブロック符号化する。

1. bytes / raw（mode 0）
2. bytes / raw DEFLATE（mode 1）
3. 文字列入力の場合だけUTF-16LE / raw（mode 2）
4. 文字列入力の場合だけUTF-16LE / raw DEFLATE（mode 3）

完成した候補は次の順で比較する。

1. 総語数が少ない
2. ブロック数が少ない
3. 全体の再使用語数 `総語数 - 異なる読みの数` が少ない
4. 各ブロックの長さ群不均衡の合計が小さい
5. 2文字語が少ない
6. 読みの総文字数が多い
7. ストリームbytesが少ない
8. modeが小さい

1と2で伝送量を最優先し、3〜6は同じ伝送量の候補から語彙の多様なものを選ぶ。

DEFLATE出力は実装によって異なり得る。同一入力から同一の正規語列が得られる保証は、
同一のDEFLATE出力が得られる場合に限る。Node.js実装とPython実装は共通ベクタに載せた
4モードの入力について正規語列まで一致させる。ブラウザなど別のDEFLATE実装を使う
環境では、語列が異なっても相互に一意復号できれば適合とする。

## 11. 表示形式

正規保存形式は、語をASCII空白`U+0020`で区切り、ブロック境界をLF`U+000A`で
表す。末尾改行は付けない。

復号器は次を受け入れる。

- ASCII空白、全角空白`U+3000`、その他のUnicode空白による語区切り
- LFまたはCRLFによるブロック境界
- 改行のない語列。この場合は35語ごと、および`ん`で終わる最終語から境界を復元する
- ひらがなとカタカナの混在

入力はNFC正規化し、カタカナをひらがなへ畳んでから符号表と照合する。符号器は
`katakanaReadings`に含まれる読みをカタカナで表示する。文字種はrankへ影響しない。

UI上の折り返しは文字列へ改行を挿入せず、CSSなどのソフトラップで行う。

## 12. 復号時の検証

復号器は少なくとも次を検証する。

- すべての語が対象辞書にある
- 各語の先頭かなが直前語の末尾かなと一致する
- `ん`で終わる語は最終ブロックの最後の1語だけである
- 通常ブロックは35語、最終ブロックは35語以下である
- 同じ読みが同一ブロック内に2回現れない
- 各経路rankが符号空間内にある
- 復元したlengthが0〜18で、通常ブロックでは1以上である
- 各ブロックのCRC-16がtype、index、length、dataについて一致する
- 第1ブロックの開始状態が8.1の計算結果と一致する
- `algorithmVersion = 1`かつ`dictionaryVersion = 1`である
- modeの予約bitが0で、modeが定義済みである
- DEFLATEおよびUTF-16LEが正しく復元できる

## 13. 設計理由

ブロック内重複禁止を経路数の再帰へ直接入れると、状態に2,748語分の使用済み集合が
必要になり、35語経路のrank/unrankは実用的でなくなる。版1では、生経路を従来どおり
小さな状態で数え、同じデータを表す余剰rankから重複のない経路を選ぶ。

辞書版1の35語生経路を5,000件無作為抽出した確認では、ブロック内重複のない経路は
約41.6%だった。さらに、128候補を使った300ブロックの無作為確認では適合候補が
0件のブロックはなく、適合候補は平均53.1件、採用候補の長さ群不均衡は平均2.99語、
最大6語だった。4,096候補では小さな入力にも長い処理時間を要したため、版1では
品質と実行時間の境界を128候補に固定する。

また、35語通常ブロックの最小容量201 bitに対し、最大ブロックフレームは168 bitで
ある。少なくとも33 bitを経路選択へ回せるため、CRC-16と語数効率を保ちながら、
重複禁止、開始かな、語長の多様性を同時に選べる。

第1ブロックの開始状態についても、2 bytesのデータ片を全探索した確認で
`CRC16(checkInput) mod 67`は67個すべての剰余へ到達する。開始かなは単なる見た目の
乱数ではなく、辞書版1の全状態が実際に選択可能である。

## 14. テストベクタ

正式実装は`A1/D1`について、少なくとも次を共通テストベクタへ収録する。

- 空バイト列
- 先頭または末尾に`0x00`を含むバイト列
- raw、DEFLATE、UTF-16LE、UTF-16LE + DEFLATEの各mode
- 複数の開始状態へ到達する実フレームと、67状態すべてへ到達できることの機械検査
- 自己ループ語を含むがブロック内重複のない語列
- 同じ語を異なるブロックで再使用する語列
- 2文字、3文字、4文字以上の各群を含む語列
- 各ブロック位置の1語破損に対し、そのブロック番号で失敗する検査
- ブロックの置換、欠落、重複でCRCが失敗する検査
- 版番号、mode、rank、length、CRC、接続、終端、ブロック内重複の拒否例
- JavaScriptとPythonの相互往復

既存の`KCB1`試作ベクタはこの最終仕様とは互換でない。正式なベクタは
アルゴリズム版と辞書版の組がファイル名または内容から明確になる形で管理する。
